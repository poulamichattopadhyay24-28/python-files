class Solution:
    def largest(self, arr):
        max1 = arr[0]
        for i in range(1, len(arr)):
            if arr[i] > max1:
                max1 = arr[i]
        return max1
def main():
    arr = [10]
    solution = Solution()
    print("The largest element is:", solution.largest(arr))
main()
